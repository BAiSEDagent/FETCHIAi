Good audit. Please tackle this as a focused v2.3/v2.1 completion pass, not a redesign.

Priority order:

1. Implement the missing LeadCard variants
   This is the most important missing piece.

Required variants:

* hot/storm/hail urgent lead = coral surface
* permit lead = parchment surface using bg-parch
* new business/default = neutral dark surface
* saved = neutral dark with ok/green accent or stripe
* won/responded = ok/green semantic state only
* expiring = neutral dark with warn/amber accent or stripe

Right now the hot/hail cards are mostly dark with coral border/score. The v2.1 board expects the hot storm lead card itself to be the loud coral brand object in the key card-forward surfaces.

2. Wire warn/amber properly
   Replace stock Tailwind amber classes with the tokenized warn color:

* bg-warn
* text-warn
* border-warn
* or CSS variable equivalents

Use warn for expiring/warning states only. This should also power the expiring lead-card stripe/accent.

3. Fix green creep
   Green is still behaving like the old brand color in several places:

* large score on Lead Detail
* Why Now panel background
* evidence source count
* score breakdown bars
* contact links
* product header/avatar accent

Green should only mean saved / won / responded / verified / healthy / success.

Please demote those generic green usages:

* Lead Detail score: use neutral text or coral only when tied to a hot lead card context
* Why Now panel: use raised/surface, not green
* Evidence/source labels and source links: use Evidence Blue
* Score breakdown bars: use neutral, Evidence Blue, or semantic only if the metric itself is verified/success
* Website/email/source links: use Evidence Blue unless it is a success confirmation
* Header/avatar: do not reintroduce green as product branding

4. Keep “skipped,” not “shipped”
   Do not touch the DB enum. The schema has skipped, and that is correct for a user passing/skipping a lead.

Treat SHIPPED on the board as a visual/wording typo unless we later add an actual outreach-delivered state. No schema change.

5. Defer “Edit summary” unless already supported
   Do not add new summary-editing behavior in this migration. If an existing edit flow already exists, style it. Otherwise defer it to a later product task.

6. Add coralDeep pressed states through the button primitive
   Low-risk polish:

* primary coral buttons should use coralDeep on hover/active/pressed
* apply this through components/ui/button.tsx so Add to My Leads / Open draft & contact / primary CTAs inherit it consistently

7. Keep the behavioral guardrails
   Do not change deck/swipe behavior.
   Do not change billing/trial/credits.
   Do not change schema.
   Do not touch provider/agent logic.
   This is still design-system completion only.

After this pass, please rerun:

* npm run type-check
* npm run build
* grep for old brand colors/tokens
* grep for stock amber usages that should be warn
* grep for bg-parch/text-parch usage to confirm permit cards exist
* visually verify My Leads, Lead Detail, Evidence, Today’s Run, and Settings.
