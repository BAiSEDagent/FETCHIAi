We are continuing Fetchi CP2 design alignment, one page at a time.

Task: Implement the My Leads page and flow to match the latest Claude Design package I uploaded.

Important context:
The previous CP2.3 pass is moving in the right direction, but the current app still does not match the richer Claude Design reference closely enough. We are now narrowing scope: do ONLY the My Leads tab/page and its direct interactions. Do not redesign the whole app. Do not touch unrelated pages unless required by shared components.

Use the uploaded Claude Design files/screenshots as the visual source of truth:
- Locked mobile bottom nav _component spec_
- Mobile 1 _ All tab
- Mobile 2 _ New tab
- Mobile 3 _ Saved tab
- Mobile 4 _ Contacted tab
- Mobile 5 _ Empty filtered state
- Mobile 6 _ Sort sheet open
- Mobile 7 _ Search active
- Mobile 8 _ Today_s Run CTA tapped
- Mobile 9 _ Card tap _ opens Lead Detail
- Desktop My Leads frames / interaction map from the uploaded design package

We really like the direction Claude Design took. Match that system closely.

LOCKED UX / ARCHITECTURE RULES

1. My Leads is the archive / pipeline.
It is not the swipe stack.

2. Today’s Run is the swipe stack / session view.
The Today’s Run CTA on My Leads routes to /app/today.
Do not create a new opportunity status called today, run, stack, snoozed, etc.

3. Use the existing opportunity statuses only:
new, saved, contacted, responded, won, lost, skipped, expired.

4. “Add to Today’s Run” or “Save” means status = saved.
Today’s Run can query new + saved opportunities later, but My Leads only displays the pipeline/archive.

5. Lead cards open Lead Detail.
No nested action buttons inside the My Leads list cards except where explicitly shown in the reference. Tap/click anywhere on the card opens /app/leads/[id].

6. Mobile bottom nav is locked.
Use the exact concept from the uploaded “Locked mobile bottom nav” reference:
Chat / Today / Leads / Map / Settings.
Fixed to bottom on mobile.
Active state = raised white/cream outlined frame + small brand-green top indicator.
Keep this style for all future mobile screens, but in this task only ensure My Leads uses it correctly.
Do not use the old blue browser focus-looking active ring.

7. Remove builder-facing copy from customer UI.
Do not show “Checkpoint 4”, “Checkpoint 6”, “Checkpoint 10”, “billing checkpoint”, “scoring agent checkpoint”, or similar internal roadmap language anywhere visible to users on this page.

8. Do not add CP4/CP5 artifacts.
No Stripe, no billing APIs, no agents/prompts, no admin routes, no schema changes.

9. Guardrails:
Do not touch:
- db/schema.ts
- db/index.ts
- db/seed.ts
- drizzle.config.ts
- design/*.html

Do not add new dependencies unless absolutely necessary. Prefer existing components, CSS tokens, Tailwind, and current app patterns.

MY LEADS PAGE REQUIREMENTS

Route:
- /app/leads

Page content:
Desktop:
- Dark left sidebar stays, but visual should match Claude reference more closely.
- Main content max width around 1120px.
- Parchment/cream app background.
- Header:
  - Small eyebrow: “Pipeline”
  - Large title: “My Leads”
  - Subtitle: “9 opportunities · 2 new today · resets May 31” or equivalent dynamic copy if real data exists.
- Top-right Today’s Run CTA card:
  - Dark card
  - Left count badge, e.g. “3”
  - Text: “Today’s Run · 3 leads queued”
  - Subtext: “Review today’s best opportunities”
  - Chevron
  - Click routes to /app/today
- Filter chips:
  - All, New, Saved, Contacted, Responded, Won, Lost, Skipped, Expired
  - Each chip shows count.
  - Active chip is dark.
  - Chips filter the visible lead list.
- Search input:
  - Filters by business name, city/location, signal type.
- Sort button:
  - Desktop opens a popover.
  - Sort options:
    - Score high to low
    - Freshness
    - Recently added
    - Business name A → Z
- Lead cards:
  - Desktop grid: two columns where space allows.
  - Cards should match Claude’s list/card variant:
    - Status pill
    - Signal token pill
    - Relative time
    - Business name
    - Location/type metadata
    - Why-now short summary
    - Contact initials avatar/name/confidence dots
    - Score large on right
    - Subtle chevron
  - Hover state:
    - subtle lift
    - softer shadow
    - reveal small “Open →” pill
  - Click opens lead detail.

Mobile:
- Match uploaded mobile My Leads frames.
- Large “My Leads” title at top.
- Subtitle: “9 opportunities · 2 new today”
- Top-right circular icon buttons for search and sort.
- Today’s Run dark CTA card.
- Horizontal scrollable filter chips.
- One-column stacked lead cards.
- Bottom nav fixed and locked as described above.
- Enough bottom padding so cards do not sit behind nav.
- On mobile:
  - Sort opens bottom sheet with dimmed background, drag handle, Reset, radio options, Apply sort CTA.
  - Search switches to active search mode:
    - back arrow
    - search input with query
    - Cancel
    - result count text like `3 results matching "plano"`
    - keyboard spacer/mock area is okay only if it is part of the visual state, but avoid breaking real mobile usability.

Empty filtered state:
- If a filter has no results, show a large friendly empty card:
  - Icon/circle
  - Title like “Nothing lost yet”
  - Copy: “When a contacted lead doesn’t close, mark it Lost. Fetchi uses that to stop surfacing similar ones.”
  - CTA: “Browse All →” which resets filter to All.

Today’s Run CTA pressed/loading state:
- On click/tap, it should route to /app/today.
- If easy, add a short transition/loading state:
  - CTA gets subtle brand-green outline
  - show “Loading Today’s Run…”
  - nav active preview can switch to Today as shown in design
Do not overbuild this. Route correctness matters more than animation.

Card tap state:
- On mobile, tapping a card should open lead detail.
- If adding pressed visual, use brand-green ring like the design.
- Do not keep tooltip annotation in production UI unless it is only dev/design preview.

DATA / PREVIEW REQUIREMENT

The current app often looks empty, which makes design hard to judge.
For this implementation, make the My Leads page visibly testable.

Use real workspace opportunities when present.
If the workspace has no opportunities or too few opportunities for visual QA, add a safe UI-only development fallback dataset for this page only.

Preferred approach:
- Create a local demo data helper for My Leads UI preview, gated to development/preview only.
- Do not mutate the database.
- Do not edit seed.ts.
- Do not change schema.
- Do not consume opportunity credits.
- Do not affect production behavior.

Demo leads should include enough records to exercise:
- New x2
- Saved x2
- Contacted x1
- Responded x1
- Won x1
- Lost x0 empty state
- Skipped x1
- Expired x1

Use names from the design:
- Parkview Office Complex — new — hail — score 94
- Addison Corporate Park — new — hail same cell — score 88
- Frisco Medical Center — saved — permit — score 86
- Westridge Retail Center — saved — hail — score 82
- Greenway Self-Storage — contacted — permit — score 79
- Apex Distribution Center — responded — hail — score 91
- Oakland Plaza Offices — won — permit — score 74
- Cedar Park Towers — skipped — hail — score 68
- One expired permit/hail lead for Expired filter

IMPLEMENTATION NOTES

Use or refactor the existing LeadCard component if possible, but make it support variants cleanly:
- variant="list" for My Leads
- preserve any existing variants needed by Chat or Lead Detail
- avoid duplicating card markup across pages

The LeadCard list variant must always show:
- status pill
- signal token
- business name
- score
- contact preview when available
- short why-now summary

Do not remove existing server actions or workspace scoping.
Do not break /app/leads/[id].
Do not break existing onboarding, chat, today, map, settings routes.

ACCEPTANCE CHECKLIST

After implementation:
1. /app/leads desktop matches the uploaded Claude desktop My Leads reference.
2. /app/leads mobile matches the uploaded Claude mobile My Leads reference.
3. Mobile bottom nav matches the locked reference and uses no blue focus-outline visual as the selected state.
4. All filter chips work.
5. Lost filter shows empty state if count is 0.
6. Search works.
7. Sort works:
   - desktop popover
   - mobile bottom sheet
8. Today’s Run CTA routes to /app/today.
9. Lead card click opens /app/leads/[id].
10. No internal checkpoint copy appears in customer UI.
11. No schema changes.
12. No CP4/CP5 artifacts added.
13. npm run type-check passes.
14. npm run build passes.
15. Provide screenshots or a clear route-by-route visual verification summary.

Stop after My Leads. Do not move on to Lead Detail or Today’s Run redesign until I approve this page.