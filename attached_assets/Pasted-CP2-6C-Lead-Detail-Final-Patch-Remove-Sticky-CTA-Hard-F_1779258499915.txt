CP2.6C — Lead Detail Final Patch: Remove Sticky CTA + Hard-Fix Hero Summary

The CP2.6B report said the hero summary and sticky CTA were fixed, but the live mobile screenshots show both issues still remain.

Route:
- /app/leads/[id]

Scope:
- Lead Detail page only.
- Do not redesign My Leads.
- Do not touch Chat, Today, Map, Settings, onboarding, billing, admin, agents, providers, or Stripe.
- No schema changes.
- No protected file edits.
- No new dependencies.

Hard guardrails:
- Do not modify db/schema.ts
- Do not modify db/index.ts
- Do not modify db/seed.ts
- Do not modify drizzle.config.ts
- Do not modify design/*.html
- No admin files
- No billing files
- No agent/provider files
- No live search/scout execution

Required fixes:

1. Hard-fix the broken hero summary

The Lead Detail hero still renders:

“Tuesday’s hail event dropped 1.”

This must be removed completely.

Do NOT rely on firstSentence() or regex sentence extraction from long why-now text for the hero summary anymore. That parsing is still failing on hail measurements like 1.8".

Instead, create a deterministic customer-facing hero summary function.

Examples:
- storm_damage / weather_hail / weather_wind:
  “Fresh storm signal matched to a commercial roof opportunity.”
- permit / building_permit:
  “Recent permit activity suggests upcoming vendor need.”
- new_business:
  “New business activity suggests a timely outreach opportunity.”
- hiring:
  “Hiring activity suggests active growth and vendor demand.”
- fallback:
  “Fetchi found a timely signal worth reviewing.”

Acceptance:
- The exact string “Tuesday’s hail event dropped 1.” must not appear anywhere.
- No hero summary may end in a dangling number, partial word, or incomplete sentence.
- Hero summary should be short, calm, and complete.

2. Remove the sticky CTA from Lead Detail mobile

The sticky “Open draft & contact” dock still visually covers content and makes the page feel blocked.

For now, remove the sticky CTA dock entirely from /app/leads/[id].

Keep the bottom mobile nav fixed and unchanged.

The primary action should live inside the Outreach Draft card only:
- Primary button: “Open draft & contact”
- Secondary button: “Edit”

This is acceptable for CP2 because the user is already on the full Lead Detail page. We can reintroduce a sticky action later if needed.

Acceptance:
- No sticky CTA overlay appears above the bottom nav.
- Bottom nav remains fixed.
- The Outreach Draft card contains the primary action.
- The Outcome card is fully reachable and readable.
- No content is covered while scrolling.

3. Adjust bottom padding after removing sticky CTA

Since the sticky CTA is gone:
- Reduce excessive bottom padding if present.
- Keep enough bottom padding so the Outcome card is not hidden behind the mobile bottom nav.
- Final page content should scroll cleanly above the nav.

4. Preserve existing page structure

Keep:
- hero
- signal token
- No claim filed chip
- Why Now card
- Evidence card
- Signal Evidence card
- Score Breakdown card
- Contact Routes card
- Outreach Draft card
- Outcome card
- bottom mobile nav with Leads active

Do not remove functional sections.

5. Verification

Run:
- npm run type-check
- npm run build

Then report:
- changed files
- confirmation that “Tuesday’s hail event dropped 1.” no longer exists in rendered code
- confirmation that the sticky CTA dock was removed
- confirmation that bottom nav remains
- confirmation that no protected files were touched

Stop after report. Do not start another checkpoint.